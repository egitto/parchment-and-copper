Sometimes, 16 decimals of precision just isn't enough. Sometimes, you want to invert a matrix using python's Fraction objects, and numpy won't handle them. That's what this is for. It's not optimized at all, so if you call it on a 1000x1000 matrix, you have only yourself to blame.
